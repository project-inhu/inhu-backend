export class SocialLoginAppResponseDto {
  /**
   * access token
   *
   * @example "this.is.accessToken"
   */
  accessToken: string;

  /**
   * refresh token
   *
   * @example "this.is.refreshToken"
   */
  refreshToken: string;
}
